function rezervasyonYap(filmAdi) {
  alert(filmAdi + " için rezervasyon sayfasına yönlendiriliyorsunuz...");
  // Örnek yönlendirme:
  // window.location.href = "rezervasyon.php?film=" + encodeURIComponent(filmAdi);
}
